[package]
name = "cortex-emulator"
version = "0.1.0"
edition = "2021"
authors = ["Cortex Formal Verification Team"]
description = "Phase 1 Software Emulator for the Cortex Spatiotemporal Capability Register (STCR) ISA (SDS v1.0)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
