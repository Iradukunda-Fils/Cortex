# CORTEX v0.7.0rc1 RELEASE CANDIDATE MANIFEST

- **Release Designation**: `v0.7.0rc1`
- **Target Branch**: `feat/external-effects-subsystem`
- **Target Release Train**: `0.7.x`
- **Package Name**: `cortex-runtime`
- **Package Version**: `0.7.0rc1` (`pyproject.toml`)
- **Release Status**: **RELEASE_CANDIDATE**
- **Production v1.0.0 Status**: **BLOCKED** (Pending Issue #23 External Security Review)

---

## 1. Scope & Delivered Capabilities

### Included Features & Kernel Capabilities
1. **External Effects Subsystem**:
   - `EffectGateway` actuation boundary with `adapter_contract.py` compatibility layer.
   - Standardized capability syntax support (`analytics:write` $\equiv$ `analytics.write`).
   - 5-plugin DAG fan-out reference application (`examples/mcp_secure_effect_app`).
2. **Sub-Gate B.2 Physical Network Containment**:
   - Linux `ip netns` network namespace isolation inside `cortex-emulator`.
3. **Sub-Gate B.3 Process Fencing & Local Restart**:
   - Epoch-monitored process incarnation fencing and benchmark suite.
4. **Python Compatibility & Type Safety**:
   - Dataclass `kw_only=True` inheritance fixes for Python 3.10 through 3.14.
   - Optional `jsonschema` loading and safe exception attribute resolution.
5. **Formal Assurance Bounds**:
   - 20 Coq formal proof modules machine-checked (0 Axioms / 0 Admits).
   - F4c verifier bounded deterministic refinement verified across 10 structural equivalence classes ($C_1 \dots C_{10}$).

### Excluded & Deferred Features
- **Cross-Node Network Clustering**: Deferred (single-node process/thread execution boundary).
- **Physical cgroup Containment in Native Python Subprocesses**: Deferred (requires spawning via `cortex-emulator`).
- **Hardware RTL Compilation Gate**: Deferred (Tier-D hardware track).
- **Issue #23 External Security Review & P0–P13 Sign-off**: Deferred (Production v1.0.0 blocker).

---

## 2. Quarantined Release Tag Record

```text
Tag Reference: v1.0.0-rc1 (Annotated Tag SHA: fa05991 / Peeled Commit: 2ec6fd0)
Status: SUPERSEDED / QUARANTINED — DO NOT PROMOTE
Reason: Created prematurely during PyPI workflow setup prior to external security audit.
```
