# Cortex Release Artifact: v0.6.0

- **Release Version**: `v0.6.0`
- **Release Title**: `v0.6.0 — Phase 8.0 Machine-Checked Refinement Proofs & WASM Profile B`
- **Release Classification**: `FORMAL_ASSURANCE_MILESTONE`
- **Target Commit SHA**: `9ad95fd` (`main`)
- **Generated At**: `2026-09-01T19:40:00Z`
- **Production Sign-Off**: **BLOCKED (Gated by Issue #23)**

---

## 1. Summary of Deliverables & Formal Scope

The `v0.6.0` release establishes the machine-checked formal proof foundation and security sandbox filters for Cortex:
1. **Phase 8.0 Coq Machine-Checked Proofs**: Zero-axiom, zero-admit Coq formal models (`Phase7Reservation.v`, `Phase8ResourceAuthorityConcrete.v`) proving simulation relation $R(C,A)$ for `ResourceAuthority` and non-resurrection property $P_{10}$.
2. **Phase 4 Gateway Concrete Refinement**: Machine-checked Coq module (`Phase4GatewayConcrete.v`) proving concrete forward simulation and atomic revalidation grant `CGStepAtomicRevalidateGrant` for issue `#32`.
3. **WASM Profile B Sandbox Integration**: Security ceiling resolver integration (`ConfigResolver`) and strict sandbox profile selection (`#33`).
4. **Gate J Property-Based Fuzzing Engine**: 13 mutation classes and independent verifier engine (`#36`) executing 100 randomized trials with zero unhandled crashes.

---

## 2. Decoupled Assurance Matrix

$$\boxed{ \text{Python Tests} \neq \text{Coq Proofs} \neq \text{RTL Simulation} \neq \text{Yosys Synthesis} \neq \text{External Security Review} }$$

| Domain | Status / Classification | Evidence Substrate |
| :--- | :--- | :--- |
| **Formal Refinement Proofs** | **FORMALLY MACHINE-CHECKED** | Coq `coqchk` Clean (0 Axioms, 0 Admits) |
| **Python Runtime Surface** | **IMPLEMENTATION VERIFIED** | 566 Unit & Conformance Tests (PASS) |
| **Gate J Adversarial Fuzzer** | **PROPERTY-TESTED** | 13 Mutation Classes / 100 Trials (PASS) |
| **Physical Isolation Controls** | **LIMITATION ACCEPTED / LOGICAL ONLY** | Landlock, Seccomp BPF, Netns, physical GPU locking, and WASM JIT remain explicit limitations |

---

## 3. Machine-Checked Proof Ledger

- **`Phase7Reservation.v`**: Machine-checked capacity safety invariants $P_1\dots P_{14}$.
- **`Phase8ResourceAuthorityConcrete.v`**: Universal forward simulation relation $R(C,A)$ and crash-safe WAL recovery theorem.
- **`Phase4GatewayConcrete.v`**: Gateway forwarding theorem and monotonic epoch rejection.
- **Coq Verification Output**: `0 Axioms, 0 Admits` verified via `coqchk`.
