name: ingestion-plugin
version: "1.0.0"
description: "Ingests external event payloads safely via Gateway-mediated MCP echo service"
consumes_events:
  - IntentEvent
produces_events:
  - CommandIssuedEvent
required_capabilities:
  - "mcp:echo"
